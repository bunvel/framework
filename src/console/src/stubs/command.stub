import {Command, type CommandArgs} from "@bunvel/framework";

export default class {{commandName}}Command extends Command {
  constructor() {
    super("{{commandName}}", "Description of the command");
  }

  async handle(args: CommandArgs): Promise<void> {
    // Implement command logic here
  }
}