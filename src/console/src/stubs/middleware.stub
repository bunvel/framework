import type { Context } from "@bunvel/framework";

const {{middlewareName}} = async (c: Context, next: () => Promise<void>) => {
  console.log("Running {{middlewareName}}");
  await next();
};

export default {{middlewareName}};

